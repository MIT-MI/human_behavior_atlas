# Extra packages for the GRPO example on UPSTREAM verl (volcengine/verl @ main).
# These sit on top of verl's own dependencies (install verl from the ../verl submodule).
#
#   conda create -n hba_verl python=3.11 && conda activate hba_verl
#   pip install "vllm>=0.11,<0.12"
#   pip install -e ../verl               # the upstream verl submodule (@ main)
#   pip install -r requirements_grpo.txt
#
vllm>=0.11.0                  # Qwen3-VL rollout needs vLLM >= 0.11
transformers>=4.57.0
qwen-vl-utils
av>=11.0.0
pyarrow>=14.0.0
pandas>=2.0.0
Pillow>=9.0.0
sentence-transformers>=2.2.0  # reward_function.py cosine-similarity reward
datasets>=2.18.0
omegaconf>=2.3.0
